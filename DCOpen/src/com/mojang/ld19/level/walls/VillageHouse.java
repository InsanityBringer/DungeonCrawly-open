package com.mojang.ld19.level.walls;

/*import java.util.Random;

import com.mojang.ld19.Art;
import com.mojang.ld19.display.Sprite;
import com.mojang.ld19.display.SpriteBody;*/

public class VillageHouse extends Wall
{
	public VillageHouse()
	{
		super();
		/*Random random = new Random(1000);
		//sprites.add(new Sprite(1.0, 0.40, 0.45, Art.window));
		//sprites.add(new Sprite(1.0, 0.40, 0.5, Art.window));
		//sprites.add(new Sprite(0.5, 0.40, 1.0, Art.window));
		//sprites.add(new Sprite(0.5, 0.40, 1.0, Art.window));
		
        int w = 6;
        int h = 8;
		
		double ws = 1.0 / (w);
        int z = 0;
        for (int x = 0; x < w; x++) 
        {
        	for (int y = 0; y < x + 1; y++)
        	{
		        double xx = x * ws + (random.nextFloat() - 0.5) * 0.03;
		        double yy = 1.0 - (y * ws + (random.nextFloat() - 0.5) * 0.03);
		        double zz = z * ws + (random.nextFloat() - 0.5) * 0.03;
		        sprites.add(new Sprite(xx, yy, zz, Art.brick));
        	}
        }*/
	}
}
